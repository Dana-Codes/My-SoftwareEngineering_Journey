function getStudentIdsSum (students) {
	const listOfStudents[];
	//check if the list of student is an array
	if(!(listOfStudents instanceof Array)) {
		return listOfStudents;
	}

	listOfStudents = listOfStudent.reduce((student) => )
}